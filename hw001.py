

# [1주차 숙제] 10진수 음수를 입력받아 8비트 '2의 보수' 2진수로 변환하기
# 1주 차 과제: [데이터 표현] 2의 보수 계산기 만들기개념 연결: 컴퓨터가 음수를 저장할 때 사용하는 '모든 비트 반전 + 1'의 원리를 코딩으로 검증합니다.
# 과제 안내문: "파이썬의 자동 변환 함수를 쓰지 않고, 비트 연산자와 조건문만으로 8비트 음수(2의 보수)를 만드는 함수를 완성하세요."


def to_twos_complement(num):
    # 입력값 검증 (8비트 정수 범위: -128 ~ 127)
    if num < -128 or num > 127:
        return "8비트 범위를 벗어난 숫자입니다."
    
    # 양수일 경우: 단순 2진수 변환 후 8자리 채우기
    if num >= 0:
        return bin(num)[2:].zfill(8)
    
    # 음수일 경우 (2의 보수 처리 필요)
    # 1단계: 절댓값을 취한 뒤 8비트 2진수 문자열로 만듭니다. (예: -5 -> 5 -> '00000101')
    abs_bin = bin(abs(num))[2:].zfill(8)
    
    # -------------------------------------------------------------
    # [TODO] 아래의 2단계와 3단계 로직을 파이썬 코드로 완성하세요.
    # -------------------------------------------------------------
    
    # 2단계: 비트 반전 (0은 1로, 1은 0으로 바꾸기)
    # 힌트: abs_bin 문자열을 순회하며 '1'이면 '0', '0'이면 '1'로 바꾼 리스트나 문자열을 만듭니다.
    inverted_bin = ""
    # 여기에 코드를 작성하세요.
    
    # 3단계: 반전된 2진수 값에 1 더하기
    # 힌트: 반전된 2진수 문자열을 다시 숫자로 바꾸어 1을 더한 뒤, 2진수 문자열로 반환합니다.
    # int(inverted_bin, 2)를 사용하면 2진수 문자열을 10진수 정수로 바꿀 수 있습니다.
    result_bin = ""
    # 여기에 코드를 작성하세요.
    
    return result_bin

# --- 테스트 코드 (학생들이 결과를 확인할 수 있는 영역) ---
print("[-5의 2의 보수 결과]")
print("예상 정답: 11111011")
print(f"학생 정답: {to_twos_complement(-5)}")

print("\n[-1의 2의 보수 결과]")
print("예상 정답: 11111111")
print(f"학생 정답: {to_twos_complement(-1)}")
